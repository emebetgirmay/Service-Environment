# Docker Hub credentials
DOCKERHUB_USERNAME=your-dockerhub-username

# Application name (should match your GitHub repo name)
APP_NAME=production-service-environment

# Image tag to deploy (use sha-<short-commit-hash> from CI)
IMAGE_TAG=sha-a1b2c3d
